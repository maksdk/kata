import { Collision, ICollsionProps } from '@core/game/components/Collision';

export class TriggerZone extends Collision {
    public constructor(props: ICollsionProps) {
        super(props);
    }
}